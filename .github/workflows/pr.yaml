name: Check PR

on:
  pull_request:
    branches:
      - feature/*
      - hotfix/*

jobs:
  build:
    name: "Check PR"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Debug
        run: |
          sleep 300
          echo "Hello, world!"
